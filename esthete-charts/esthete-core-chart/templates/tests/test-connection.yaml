apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esthete-core-chart.fullname" . }}-test-connection"
  labels:
    {{- include "esthete-core-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esthete-core-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
